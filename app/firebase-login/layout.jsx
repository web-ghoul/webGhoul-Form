import { Box } from '@mui/material'
import React from 'react'

const FirebaseLoginLayout = ({children}) => {
  return (
    <Box>
      {children}
    </Box>
  )
}

export default FirebaseLoginLayout
