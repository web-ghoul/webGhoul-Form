import { Box } from '@mui/material'
import React from 'react'

const FirebaseRegisterLayout = ({children}) => {
  return (
    <Box>
      {children}
    </Box>
  )
}

export default FirebaseRegisterLayout
